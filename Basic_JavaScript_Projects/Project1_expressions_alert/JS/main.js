var A = "\"Hello, world!\"";      //Defining variable and giving it a string value.

window.alert(A)     //Exacuting window.alert and providing it with the variable A to be displayed.

var Sent1 = "This is the beginning of the string"       //Defining a variable with the beginning of a string.
var Sent2 = " and this is the end of the string"        //Defining a variable with the end of a string.

document.write(Sent1        //Exacuting document.write and concatenating a string using two variables for display.
    + Sent2) 

document.write(7+3)     // Exacuting document.write where the value of an expression will be displayed.

